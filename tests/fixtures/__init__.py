"""Test fixtures for receivers package.

This module provides test data and fixtures that capture the current behavior
of the receivers package before refactoring. These fixtures serve as regression
tests to ensure that refactored code produces identical outputs.
"""